apiVersion: v1
kind: Service
metadata:
  name: ml-service
  labels:
    app: wordcraft-ml
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  selector:
    app: wordcraft-ml
